{
  "compilerOptions": {

    /* Lenguaje y Entorno*/
    "target": "ES6",                                      // Transpila a ES6

    /* Modulos */
    "module": "commonjs",                                 // Utiliza módulos CommonJS (por defecto para Node)
    "rootDir": "./src",                                   // Directorio raíz para los archivos fuente
    "outDir": "./dist",                                   // Salida de los archivos compilados
    "esModuleInterop": true,                              // Interoperabilidad con módulos ES6
    "forceConsistentCasingInFileNames": true,             // Fuerza una nomenclatura consistente

    /* Chequeo de tipos */
    "strict": true,                                     // Habilita todas las comprobaciones de tipo estrictas

    /* Completeness */
    "skipLibCheck": true                                 // Omite la verificación de los archivos de las bibliotecas
  },
  "include": [
    "src/**/*.ts"           // Incluye todos los archivos .ts en el directorio src
  ],
  "exclude": [
    "node_modules"          // Excluye la carpete node_modules
  ]
}
